#include <stdio.h>

int add_in_tab(int tab[i],int n)
{
    printf("git postition are want to add")
   int i = 0;
   while(c < n)
   {
       if(p == i)
       {

       }
   }
}
int showoptions()
{
    int option;
    printf("1 : add value in table");
    prntf("2 : search in table ");
    printf("3 : delet in table");
    scanf("%i",&option);

    return option;
}

void (int tab[100],int n)
{
    int i = 0;
    while (i < n)
    {
        printf("%i | ",tab[i]);
        i++;
    }
}

int main()
{
    printf("donner la taille de tableau");
    scanf("%i",&n);
    

    while (c != 0 )
    {
        option = showoptions();
        if(option == 1)
        {
            add_in_ta(tab,n);
        }
    }
    

    int tab[100];
    show_data(tab,n);
    
}